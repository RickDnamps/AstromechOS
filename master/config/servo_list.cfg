# Dome servo definitions (PCA9685 0x40 on Pi 4B)
# Format: name = channel,min_pulse,max_pulse,default_pos
# min/max in microseconds (µs), default_pos in 0.0-1.0

[dome]
# Example — adjust to match actual wiring
# horiz_tilt = 0,600,2400,0.5
# vert_tilt  = 1,600,2400,0.5
