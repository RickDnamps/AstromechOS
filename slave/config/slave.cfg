[audio]
audio_channels = 6

[i2c_servo_hats]
# I2C addresses of PCA9685 servo HATs on the Slave Pi.
# Motor HAT (TB6612 @ 0x40) is NOT a servo HAT — do not list it here.
# Add a second address (e.g. 0x42) if you install a second servo HAT.
slave_hats = 0x41
