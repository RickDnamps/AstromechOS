# AstromechOS — Audit History

> Historical record of systematic bug audits and security hardening passes.
> CLAUDE.md keeps only the latest phase row; everything else lives here.
>
> Each row corresponds to a coherent batch of commits. For details on a
> specific pattern or fix, prefer `bd memories <keyword>` over reading
> this file — beads memories are the indexed knowledge base.

## Phase progression

| Phase | Description | État |
|-------|-------------|------|
| 1 | UART + watchdog + audio + Teeces + RP2040 + déploiement | ✅ |
| 2 | VESCs + moteur dôme + servos MG90S | ✅ |
| 3 | 40 séquences comportementales → migrées .chor | ✅ |
| 4 | API REST + dashboard web + Android | ✅ |
| 4+ | BT gamepad + CHOREO timeline + VESC diagnostic | ✅ |
| 4++ | Caméra USB autodetect + BT battery/RSSI + keepalive + admin timers | ✅ |
| 4+++ | Choreo admin guard · Arms body-panel auto-dispatch · Settings sidebar refonte · Choreo toolbar/footer | ✅ |
| 4++++ | Sequences tab redesign : catégories + emoji + pills/grid · Behavior engine ALIVE | ✅ |
| 4+++++ | Arms : séquence panel→delay→arm · all_body arm-aware · labels Calibration dans Choreo · auto-label prefix-safe | ✅ |
| 4++++++ | GPIO dome button retiré · Rollback web UI · Hardware config UI (HATs + uart_lat) · repo_url éditable | ✅ |
| 4+++++++ | CSS variable system · 8 built-in themes · Blueprint light · theme customizer with live preview · sci-fi fonts | ✅ |
| 4++++++++ | Topbar clean · Cockpit pills HB/UART/BT · pill SLAVE · E-STOP red overlay · STATUS button toujours à jour | ✅ |
| 4+++++++++ | Cockpit SERVICES diagnostic : Dome/Body Servo HATs · Motor HAT I2C probe · RP2040 Screen health | ✅ |
| 4++++++++++ | Slave Motor HAT addr configurable UI · SHUTDOWN UART cmd · system buttons 2×3 grid (Reboot/Shutdown × Master/Slave/Both) | ✅ |
| 4+++++++++++ | Rebrand R2D2→AstromechOS · dossier Pi `/home/artoo/astromechos` · services `astromech-*` · cache SW versionné par commit | ✅ |
| 4++++++++++++ | VESC safety helper unifié (`master/vesc_safety.py`) · slave boot banner + auto-resync VCFG/VINV · paired-side CAN liveness · telemetry key fix curr→current | ✅ |
| 4+++++++++++++ | E-STOP freeze · Reset stow lent (slew=3) · ChoreoPlayer event-driven scheduler · arm Timers respectent stop_flag · loop reset complet · /choreo/play locked | ✅ |
| 4+++++++++++++++++ | E-STOP servo freeze pattern (driver `_frozen` flag + UART `FREEZE:1/0`) · UI `emergencyStop()` ne race plus close_all contre le freeze · Reset E-STOP utilise `DOME_SERVOS` (fix ImportError silencieux) | ✅ |
| 4++++++++++++++ | Heartbeat ACK age tracking · UART buffer keep-trailing 256B · /status local.cfg cache TTL · StatusPoller in-flight · heartbeat visibility-aware | ✅ |
| 4+++++++++++++++ | UART RTT histogramme + `/diagnostics/uart_rtt` + bouton MEASURE/APPLY dans Settings · hot-swap `body_servo_uart_lat` (no reboot) | ✅ |
| 4++++++++++++++++ | Choreo full audit (2026-05-13, 33 items): scroll fix · path traversal · XSS escape · atomic .chor write · audio race lock · parallel servo reset (14× faster) · newChor schema · listener leaks · 11 patterns. Commits `31886ff..d38cf0f` | ✅ |
| 4+++++++++++++++++ | Drive full audit (2026-05-14, 44 items): estop_active gate · uncancellable safety ramp · BT keep-alive clear · Pointer Events joystick · multi-touch pointerId · NaN guard · vesc_safety atomic snapshot · CAN recovery hysteresis · stow_in_progress gate · arcade ratio normalize · kids cap server-side · HUD rAF batching · 17 patterns. Commits `98c7e16..ceb0d96` | ✅ |
| 4++++++++++++++++++ | Audio Phase 1 critical fixes (2026-05-14, 6 items): path traversal `/audio/file/<sound>` (unicode rejected) · upload TOCTOU + realpath + cross-category dedup + overwrite guard · SFTP creds from `[deploy] slave_password` (no more hardcoded) · atomic SFTP via `.tmp + posix_rename` · mpg123 zombie reaper (`_reap` helper) · XSS in category pills + sound buttons via createElement+dataset. Commit `5cdf462` | ✅ |
| 4+++++++++++++++++++ | User-reported follow-up bugs (2026-05-14, 6 items): Calibration UI typing wiped every 2s · servo angle/label files were tracked despite `.gitignore` · VESC reconnect loop never ran if initial setup failed · `VESC_G`/`VESC_D` → `VESC_L`/`VESC_R` (legacy French keys) · stale VESC telem persisted after disconnect (`_fresh_telem(side)` helper) · VESC safety banner pushed Drive bottom bar off-screen. Commits `03290a8..52218fd` | ✅ |
| 4++++++++++++++++++++ | Audio HIGH (12) + MEDIUM (13/15) batch (2026-05-14): duration cache mtime · `_category_avg_duration_ms` for /audio/random pacing · `_last_random` no-twice · `/audio/scan` mtime cache · amixer setup-probe · queue.Queue maxsize=64 drop-oldest · `d.status==='ok'` ghost-play fix · `_sound2cat` Map memo · indeterminate progress bar · `_metadataAudio` single-instance + abort · `title` for long names · live filter input · `_selectEpoch` race token. Commits `1200edf..3a2611f` | ✅ |
| 4+++++++++++++++++++++ | MP3 duration ms-accurate via mutagen (2026-05-15): birthday.mp3 played 39s but UI badge stuck for 67s. Old `size*8/192kbps` broke for non-192 files. Primary source now `mutagen.mp3.MP3.info.length` (frame-counted). Header parser kept as fallback. `python3-mutagen` provisioned by setup_master.sh + idempotent `update.sh` step 1c. Verified 321/321 files within ±0.5s. Commits `032497c..46171c8` | ✅ |
| 4++++++++++++++++++++++ | Sequences full audit (2026-05-15, ~50 items): **CRITICAL** — `_admin_auth.py` `require_admin` decorator + `X-Admin-Pw` header on mutation endpoints · category id allowlist · emoji 16-char cap · label 64-char cap · `_chor_file_lock` + `_atomic_write_chor` on category delete · pills + cards built via createElement (no innerHTML interpolation). **HIGH** — `safe_play()` helper unifies stop→reset→play under `_play_lock` · ChoreoPlayer telem reads `fresh_telem_pair()` · /status snapshots `is_playing()` once. **MEDIUM** — `/choreo/list` + `/choreo/categories` mtime caches · DELETE refuses if playing · reorder reports `conflict:bool` · long-press per-pointerId Map + 8px threshold. **LOW** — fsync · skip hidden files · `_ORDER_FALLBACK = 999` · preserve operator casing. Plus arm-block timeline resize (3-field schema) + sounds_index corruption recovery. Commits `b66a1d2..bcb5c5c` | ✅ |
| 4+++++++++++++++++++++++ | Settings full audit (2026-05-15, **87/130 items fixed + 43 documented**): **CRITICAL (51/51)** — `@require_admin` on 44 endpoints in 8 blueprints · 6 stored-XSS sinks hardened · nmcli SSID rejects `-` prefix + control chars · `_sync_slave_hat_cfg` / `_sync_audio_channels` use `write_cfg_atomic` · `bt_config.json` save tmp+rename+fsync · behavior_bp + servo_bp share `_cfg_write_lock` · `deploy_controller` `import time` (NameError waiting). **HIGH (15/26)** — admin_verify `hmac.compare_digest` + per-IP rate-limit · upload_icon leading-dot + realpath · `_write_key` filters control chars · `_resolve_slave_ssh_target()` from cfg · `/diagnostics/logs` + `/diagnostics/uart_rtt` admin-only · servo panel labels char-filtered · behavior choreo names validated against on-disk files · `write_cfg_atomic` chmod 0o600 + fsync. **MEDIUM (12/23)** — `_normalise(dotkey, raw)` per-key validators · `.svg` removed from `_ALLOWED_EXT` · journalctl timeout 5s→2s. **LOW (9/30)** — stdlib imports consolidated · `silent=True` on every `get_json()` · NaN guards. Plus hotfix `054c383` — `api()` attaches `X-Admin-Pw` on EVERY method (was non-GET only). Commits `319b74b..379f574` | ✅ |
| 4++++++++++++++++++++++++ | Remaining tabs audit (2026-05-15, Calibration + Behavior + Camera + Cockpit/Status, **23/91 fixed + 68 documented**): **CRITICAL (6/6)** — XSS sinks DOM-rebuilt (behavior pills, ServoPanel.render, _renderHatBlocks) · `camera.env` + angle JSON atomic writes · `_angles_lock` shared by /servo/settings + /servo/arms · `BehaviorEngine._write_cfg` acquires `_cfg_write_lock`. **HIGH (12/22)** — camera restart debounced 2s · upstream.close() in finally · _read_cam_env tolerant int parse · status_bp camera_bp import narrowed to ImportError · behavior config range validation + idle_mode allowlist · Slave health poll reads [slave] host · servo angle SCP backgrounded · _safe_position helper · servo name allowlists. **MEDIUM (5/38)** — cockpit `_updateVitals` fingerprint diff · `:focus` guards on arms-count + cameraConfig · BehaviorEngine config snapshot-on-write. Commits `9520f40..18fa258` | ✅ |
| 4+++++++++++++++++++++++++ | Settings/Interface UX polish + VESC bench bridge + keyboard tab-scoping (2026-05-15, user-reported): **Interface tab** — SVG icons split allowlist (`_ALLOWED_LIST_EXT` incl. .svg vs `_ALLOWED_UPLOAD_EXT` no svg) · custom theme buttons `onclick = applyTheme(t.id)` · theme preview matches live `.tab` pixel-for-pixel · ADMIN topbar gear icon swap (lock→gear matching modal, 18×18). **VESC bench bridge** — Master `/vesc/bench_mode` emits `VCFG:bench:1/0` UART → Slave's `vesc_driver.handle_config_uart` parses → `drive():237` bypasses `_can_lost` when bench on. Single-VESC bench testing now possible. **Keyboard scope** — WASD + Arrow keys gated to Drive tab in `_handleKeys()` via `document.querySelector('.tab.active').dataset.tab`. VESC Test Mode has its own `vescTest.onKey()` pipeline. Tab switch while key held auto-fires `driveStop()`. **VESC Test Mode auto-disable** — `switchTab()` + `switchSettingsPanel()` call `vescTest.toggle()` on leaving VESC sub-panel. Commits `50115a1..9f325a9` | ✅ |
| 4++++++++++++++++++++++++++ | Shortcuts feature v1 + iterative UX fixes (2026-05-15, user-requested): operator-configurable macro buttons on the Drive tab. New `shortcuts_bp` (admin-gated POST · LAN-open trigger · atomic JSON write under `_SHORTCUTS_LOCK` · per-action-type validation: arms range + body/dome panel allowlists + on-disk choreo regex + `_safe_sound_path` + category membership). Frontend `shortcutsEditor` (Settings panel with emojiPicker integration · auto-fill icon/label from choreo emoji/name with `_iconAuto`/`_labelAuto` locks · cascade-aware target dropdowns from `_servoCfg.panels` + `armsConfig._labels`). `shortcutsRunner` (Drive tab overlay split symmetrically over each joystick · green pulse on `is-playing` · toggle re-press kills active choreo/sound · per-axis choreo lockout via `choreo_uses_propulsion`/`choreo_uses_dome` flags on `tracks` inspection). Toolbar: 🎛️ at top of Settings sidebar (was ⚡). Cascade rename/delete hooks from `choreo_bp` → `shortcuts_bp.cascade_rename`/`cascade_delete` so renaming a .chor follows through every targeting shortcut. CapsLock indicator on admin/lock modals. `/lock/unlock` server-side password verify replaces the hardcoded JS `'deetoo'`. Drive lockout per axis: `propulsion` track → left joystick veiled + WASD gated; `dome` track → right joystick X-clamped + ←→ gated. Wrap labels to 2 lines · word-break for long names. Audit found H-1 (audio toggle-stop didn't sync `reg.audio_playing`) + 3 MEDIUM + 2 LOW — all fixed same session. Commits `3f391b6..b8d26f5` | ✅ |
| 4+++++++++++++++++++++++++++ | Settings menu 5-agent audit + 5-batch fixes (2026-05-15, user-requested 100% pass): five `superpowers:code-reviewer` agents in parallel covered settings_bp/network/deploy/system · audio/interface/theme · vesc/calibration/arms · behavior/camera/bt/lock · teeces/diagnostics/cross-cutting. Total: **7 CRITICAL + 20 HIGH + ~32 MEDIUM + ~24 LOW fixed**. **CRITICAL** — bench_mode not re-pushed on slave BOOT:READY · ChoreoPlayer `safe_play` missed estop+stow gates · choreo propulsion-stop bypassed ramp · Lock Mode unlock pwd hardcoded JS `'deetoo'` (server-side now) · `/lock/set` not persisted (now `local.cfg [security]`) · `/bt/config` accepted arbitrary mappings (E-STOP could be silently disabled) · `/bt/config` 500 on TypeError. **HIGH** — teeces_bp ENTIRE blueprint unauthenticated · `/teeces/text` `\r` injection · `/teeces/raw` no allowlist · vesc_bp + audio_bp own `_cfg_lock` instead of shared · 56 `request.get_json` patterns crashed on JSON array body · `MAX_CONTENT_LENGTH=16MB` global · slave-HAT keys bypassed `_normalise` (section injection into slave.cfg) · BT MAC unvalidated before bluetoothctl argv · `/diagnostics/stats` not admin-gated · power_scale boot reload no finite check. **MEDIUM** — sounds_index.json atomic + fail-closed on corruption · `_INDEX_CACHE`/`MTIME` race · arms sequences honor stow_in_progress · angle JSON chmod 0o600 · `/vesc/can/scan` admin-gated · `X-Forwarded-For` dropped from rate-limit · admin password 128-char cap · behavior choreo regex tighter · Flask 500 logs traceback · cam.env 0o600 · `/status` atomic VESC snapshot · icon ASCII-only + magic-bytes · hostname RFC-1123 regex · BT device name sanitize · evdev OSError-narrow · `/diagnostics/logs` secret-redact + 4KB cap · UART decode-error counter · CRC log rate-limit · `time.monotonic` for VESC telem (NTP-safe). **LOW** — `escapeHtml` adds `'`/`` ` `` · category 64 + sounds-per-cat 512 cap · theme localStorage 16 cap · CAN_LOST synthetic v_in→0 · `_btctl` MAC defense-in-depth · `_ka_count` wrap · `setKidsSpeed` POSTs immediately · WPA2 password 63-char cap · BT save skip-if-unchanged · VESC cfg audit log. 3 bugs caught by live test that py_compile missed: `_persist_lock_mode` swapped args + bad import · BT mappings keys/values inversed · `_safe_choreo_path` tuple unpack. Commits `b8d26f5..40427a0` | ✅ |
| 4++++++++++++++++++++++++++++ | All-Settings-sub-panels deep audit (2026-05-16, **130 fixes / 21 commits / 259 tests passed**): per-sub-panel triple-agent audit (bugs / WOW / perf+edge) then batched implementation. **Network** (21/3 batches) — SSID/PSK validation parity, /wifi/scan rate-limit + admin gate, hotspot rollback on nmcli fail, GET /settings field masking, password eye toggle, PSK strength meter, current-SSID badge, band display, bidi-override reject. **VESC** (8/2) — /telemetry stale-detection broken (time.time vs monotonic), 3× `bool('false')` truthy bugs, live power scale tier, reset defaults, E-STOP auto-disables Test Mode, bench danger pulse. **BT Gamepad** (18/3) — _send_dome clock mismatch, disabled-BT-couldn't-E-STOP, _trigger_estop delegates to system_estop, update_cfg lock, inactivity flag re-arm, duplicate binding rejected, /bt/scan/devices admin gate, chmod 0o600, lock+bench pills, inactivity pause warning, REMOVE confirm. **Camera** (17/2) — release toggle-OFF sent no token (401 → orphan), _active_streams TOCTOU, preview at 5s (was 2.2s race), preview takes own token, skip-if-unchanged write, quality grid validator, /camera/snapshot endpoint, status pill 3-state. **Shortcuts** (10) — play_animation invisible (DATA LOSS RISK), _act_play_choreo ignored safe_play() return, _ShortcutBusy/Refused exceptions, per-sid debounce+lock, defense-in-depth path validation, state.clear inside lock, /status exposes shortcut_states cross-tab. **Theme/Interface** (12) — robot_name allowlist (rejects `:` for _lsKey), applyTheme default fallback, swatch CSS-injection prevention, cross-tab storage event sync, _R2_LOGO_SVG currentColor, WCAG contrast indicators. **Deploy** (8) — branch RCE prevention (--upload-pack), Master service restart after deploy (was stale Python), DeployController.reload_cfg, safety gate before deploy, _deploy_lock, --ff-only. **System** (16/2) — _system_action_guard on ALL reboot/shutdown (was: arms fall mid-choreo), idempotency lock, REBOOT BOTH retransmit + 2.5s sleep, admin pwd rate-limit + hmac.compare_digest, ANYONE-ADMIN via control-char pwd fix, adminGuard._token re-key, default password warning banner. **Diagnostics** (10) — in-flight guards, document.hidden gate, generation token, i2c_scan lock, extended secret regex (Authorization/Bearer/Cookie), RTT min samples ≥20. **Arms** (9) — USER-REPORTED PAIN FIX: custom label preservation on revert (B1, prefix-aware) · _safe getint/getfloat · count-bounded save · duplicate servo reject · NaN/Inf delay · auto-label inside lock · UI delay 0 falsy fix · live conflict warning · destructive count-reduction confirm. **Post-session cleanup** — RESTART badges removed (operator: "trop d'information"), **STATUS pill green-on-reload bug** (teecesController._applyFLDMode TypeError silently killed StatusPoller._pollOnce → typeof guard added), `?v={{ version }}` cache-bust on app.js + style.css, /favicon.ico handler, /bt/scan/devices admin-guard on auto-refresh. Commits `40427a0..fc281ef` | ✅ |
| 4+++++++++++++++++++++++++++++ | BT Gamepad — Custom Button Actions feature + sub-panel UX refonte (2026-05-16, user-requested + audited): **Custom Button Actions** (commits `3e515d9..0a7ebd9`, ~13 commits) — operator binds ANY controller button to one of `arms_toggle | body_panel_toggle | dome_panel_toggle | play_choreo | play_sound | play_random_audio | none`. Per-MAC profile storage in `bt_config.json['device_profiles'][MAC]['custom_button_mappings']`. **Press-to-capture workflow** (`/bt/capture/start` → `_capture_mode` under `_capture_lock` → frontend polls `/bt/capture/state`). Validation by type at SAVE AND TRIGGER (defense-in-depth). Auto-profile via `_ensure_device_profile(MAC, name)` on first connect. Legacy fixed buttons removed (Dome/Body/Random sound). Pairing UI collapsed into `<details>` dropdown (rarely used). Compact 2-col grid. Custom device profile selector (cards with full name + MAC, replaced cramped `<select>`). Camera-axis mapping (future feature, mapping saved for later). **BT header premium refonte** (commits `3d8925b/ad0bc5e/0a7ebd9`) — grid layout `[icon 56x40] [info: STATUS / name / MAC pill ⛓] [ON toggle] [vitals: BAT bar + SIG dBm stacked]`. New SVG icon: stylised silhouette with grips + status LED top (CSS keyframe pulse 2s when `.gamepad-icon.connected`) + D-pad cross + 4-button diamond. `is-connected` class on `.bt-status-row` cascades top-accent line color + MAC pill green tint. **MAC fallback chain** in `_resolve_device_mac()` — NVIDIA Shield + several 8BitDo don't expose `evdev.uniq`, fallback to `bluetoothctl devices Connected` name-match (exact > startswith[:24], log + return None on ambiguity). Helper called BEFORE `with self._lock:` in `_reconnect_loop` (subprocess 2s blocks shared lock with `_do_drive`/keep-alive → MotionWatchdog 800ms race-cut risk). **Architectural fix** — `BTControllerDriver.get_status()` now surfaces `active_device_mac`. Was monkey-patched in `/bt/status` after `get_status()`, so the `**bt_status` spread in `status_bp` (main poller) never carried it → header MAC pill stayed hidden even when fully resolved. RULE codified in CLAUDE.md: any field read from `/status` MUST be in `driver.get_status()`. Audit post-feature found 1 MEDIUM (lock-held subprocess) + 2 LOW (ambiguous startswith match · unscoped `_setUI` querySelector) — all fixed in `ad0bc5e`. Commits `3e515d9..0a7ebd9` | ✅ |
| 5 | Caméra USB stream ✅ · caméra permanente commandée · suivi personne AI | 📋 |
